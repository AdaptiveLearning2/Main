# Findings the `Secret scan` job may skip, by fingerprint (`commit:path:rule:line`),
# so an entry silences one match in one commit, not the rule or the file.
# Only for things that are not credentials; a committed credential is rotated, not silenced.
# Never quote the matched text here: that makes this file a finding itself.

# Placeholder bearer token in a curl example in the mac setup guide; never a live value.
b233397c940fba807854946044249a6e11b5d70a:DEVELOPER_SETUP_MAC.md:curl-auth-header:113
