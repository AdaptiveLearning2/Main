# Supabase
.branches
.temp

# Seed data — dumped from the remote DB; contains real user data, never commit
seed.sql

# dotenvx
.env.keys
.env.local
.env.*.local

# Dashboard SQL-editor snippets, synced down by the Supabase CLI. Local
# scratch tied to one machine and one dashboard session -- not project config.
snippets/
